# NX Trust

بنية تحقق وتأهيل وثقة متعددة المستأجرين للسوق السعودي. تسحب بيانات من جهات رسمية عبر مزودين، وتحفظها كإفادات موقوتة لا تُستبدل، وتبني منها ملفات كيانات حية مع أدلة وقرارات وتنبيهات.

**اقرأ `docs/00-START-HERE.md` قبل أي تغيير معماري.**

---

## القواعد التي لا تُكسر

هذي ليست تفضيلات. كسر أي منها يُعتبر خطأ يجب إصلاحه فوراً.

1. **جدول `attestations` لا يقبل UPDATE ولا DELETE.** كل معرفة جديدة INSERT. الاستثناء الوحيد: ضبط `superseded_by` مرة واحدة على الصف السابق. الحذف حصراً عبر مهمة الاحتفاظ بدور منفصل.

2. **لا استعلام يعبر المستأجرين.** أي استعلام بلا `tenant_id` خطأ، بما فيه التقارير الداخلية. الإحصاءات من عدادات مجمّعة لا من البيانات الخام.

3. **RLS مفعّل على كل جدول يحمل `tenant_id`.** الاعتماد على `WHERE` في الكود وحده غير مقبول. طبقتان لا واحدة.

4. **لا يُخزَّن رقم هوية أو معرّف شخصي نصاً صريحاً.** هاش HMAC بمفتاح لكل مستأجر للبحث، ومشفّر للعرض. ولا يظهر في السجلات ولا رسائل الخطأ ولا النسخ الاحتياطية.

5. **لا اسم مزوّد في أي استجابة عامة.** الحقل المكشوف `authority` وهو الجهة الرسمية. `source` داخلي فقط.

6. **كل حقل في نموذج البيانات يحمل `authority` و`observed_at`.** الحقل الذي لا يملكهما مرفوض. لا ملاحظات حرة، لا مهام، لا جهات اتصال، لا حقول يدخلها المستخدم. الاستثناء الوحيد `review_cases.decision_note`.

7. **كل POST يقبل `Idempotency-Key` ويحترمه.** نفس المفتاح يُرجع نفس النتيجة ورسماً واحداً.

8. **المنتجات تُعرَّف في قاعدة البيانات لا في الكود.** إضافة منتج تحقق = صفوف في `products` و`product_steps` و`step_field_map`. لا نشر برمجي.

9. **لا تحسين قبل قياس.** لا تقسيم جداول، ولا جداول إسقاط، ولا كاش، قبل أرقام فعلية تثبت الحاجة.

10. **لا اعتمادات في قاعدة البيانات.** `credential_ref` مؤشر إلى KMS فقط. قائمة حجب صريحة في طبقة التسجيل ومعالج الأخطاء.

11. **بوستجرس حقيقي في الاختبارات.** لا SQLite ولا قاعدة في الذاكرة ولا mocks لطبقة قاعدة البيانات. RLS ميزة خاصة ببوستجرس، واختبار العزل على قاعدة أخرى لا يثبت شيئاً. استخدم Testcontainers مع نفس إصدار بوستجرس المستخدم في الإنتاج.

12. **دور تطبيق منفصل عن دور المالك.** دور المالك يتجاوز RLS افتراضياً، فتمر اختبارات العزل زوراً. أنشئ دورين: دور المالك يملك الجداول ويشغّل الترحيلات فقط، ودور التطبيق بلا BYPASSRLS وبلا ملكية.

---

## المعمارية

```
apps/api        خدمة الـAPI العامة (Fastify)
apps/console    واجهة المستخدم (Next.js, RTL)
apps/worker     المهام المجدولة: المراقبة، الاحتفاظ، التقارير
packages/core   منطق المجال: الإفادات، الحداثة، القرار، التسعير
packages/db     المخطط والترحيلات و RLS
packages/providers  واجهة VerificationProvider وتنفيذاتها
```

**قواعد معمارية:**

- منتج واحد متعدد المستأجرين. **لا فرع كود لكل عميل.** اختلاف النشر متغيرات بيئة لا قاعدة كود ثانية.
- كل استدعاء مزوّد يمر عبر واجهة `VerificationProvider`. لا استدعاء مباشر من طبقة المجال.
- طبقة تطبيع إلزامية: استجابة المزوّد تتحول إلى إفادات وكيانات عبر `step_field_map`، لا عبر كود خاص بكل منتج.
- **استجابتنا لا تشبه استجابة أي مزوّد.** مخططنا، أسماء حقولنا، وحقول لا وجود لها عندهم: `decision`, `entity_id`, `freshness`, `evidence_url`.
- `mode` في `tenant_provider_binding` على مستوى (مستأجر × مزود) لا مستوى المستأجر.
- الاعتماد يُقرأ من `tenant_provider_binding` لا من متغير بيئة عام.

---

## حزمة التقنية

- TypeScript, Node 20+, pnpm workspaces
- Fastify للـAPI، Zod للتحقق من المدخلات
- PostgreSQL 15+، Drizzle ORM مع SQL خام حيث يلزم
- Next.js App Router للكونسول، RTL كامل
- Vitest للاختبارات، Testcontainers لاختبارات قاعدة البيانات
- OpenAPI 3.1 مولّد من الكود، لا مكتوب يدوياً

---

## حراس المعمارية

هذي الاختبارات تُكتب قبل المنطق وتبقى خضراء دائماً. فشل أي منها يوقف الدمج.

```
test/guards/
  01-attestations-immutable      UPDATE و DELETE يرفعان خطأ
  02-tenant-isolation            مستأجر أ لا يرى صفاً لمستأجر ب
  03-idempotency                 نفس المفتاح = نفس النتيجة ورسم واحد
  04-skipped-not-billed          خطوة SKIPPED برسم صفر
  05-no-plaintext-identifiers    لا معرّف صريح في أي عمود أو سجل
  06-no-provider-leak            لا اسم مزوّد في أي استجابة عامة
  07-ttl-change-is-inert         تعديل مدة الصلاحية لا يمس أي إفادة
  08-partial-success             فشل خطوة غير إلزامية يُرجع PARTIAL لا ERROR
  09-sandbox-isolation           تشغيل في بيئة الاختبار لا يمس المساحة الحقيقية، والمستند التجريبي موسوم
  10-price-covers-cost           لا سعر مشحون أقل من تكلفته، والتكلفة لكل نداء لا لكل منتج
```

---

## الواجهة

الشكل كله (الألوان، الخطوط، الأنصاف، الظلال، المسافات، النصوص) تحكمه حزمة التصميم في `design_handoff_verification_platform/` وقواعدها الملحقة في آخر هذا الملف تحت «قواعد التصميم لهذا المستودع». عند أي تعارض في الشكل بين هذا القسم وتلك القواعد، قواعد التصميم هي المرجع. ما يبقى هنا قواعد سلوك:

- RTL كامل. الأرقام والمعرّفات والآيبان LTR داخل عنصر `dir="ltr"`.
- **المعرّفات على الشاشة (ADR-127، ADR-128، ADR-130):** كل معرّف يُعرض كاملاً في الكونسول للمشترك صاحب العميل: السجل التجاري والرقم الموحد، والهوية الوطنية والإقامة، ورقم وثيقة العمل الحر، ووثيقة الطرف (`PARTY_ID`)، والآيبان مقسّماً أربعاً أربعاً. وفي الـAPI العام والملف المشارك برابط يبقى كل معرّف مقنّعاً. والتخزين والسجلات والأخطاء كما تنص القاعدة 4 بلا تغيير.
- **كل ما تعيده الجهة (ADR-128):** يُقرأ كل حقل في إجابة المصدر ويُحفظ بصف في `step_field_map`، ويُعرَّف في `field-catalogue.ts` باسمه وجزئه وتنسيقه. حقيقة الشخص داخل شركة (مسار ينتهي بمعرّف الشركة) لا تُعرض في أقسام ملفه.
- **تمييز إلزامي:** "منتهي الصلاحية" محايد، و"تغيّر مرصود" أو "تعارض" بلون التنبيه الكهرماني (`tag-accent`، ADR-122). لا تخلطهما. والفشل والخطأ بالأحمر (`tag-critical`).
- لا يُعرض حقل بلا ختمه الزمني ومصدره. يكفي أن يظهرا مرة واحدة على مستوى القسم حين تأتي حقوله كلها من تحقق واحد، كما في شاشة ملف العميل.
- زر أساسي واحد في رأس كل شاشة. الاستثناء الوحيد زر «تحقق» داخل قسم فيه تعارض، كما في شاشة ملف العميل.

---

## الأسلوب

- لا em dash في أي نص. فواصل أو نقطتان أو نقطة وسطى.
- التعليقات والأسماء البرمجية بالإنجليزية. واجهة المستخدم ورسائل الخطأ بالعربية والإنجليزية.
- رموز الأخطاء خاصة بنا (`NX-4021`) لا رموز المزوّد، مع `retryable` صريح و`request_id`.
- كل تحويل عملة بالريال السعودي، والأسعار تُخزَّن بلا ضريبة والضريبة تُحسب عند العرض.

---

## سير العمل في كل جلسة

1. اقرأ هذا الملف و`docs/progress.md`
2. **اعرض خطتك للوحدة المطلوبة قبل كتابة أي كود، وانتظر الموافقة**
3. نفّذ وحدة واحدة فقط
4. اكتب اختباراتها وشغّلها حتى تنجح
5. شغّل حراس المعمارية كاملة
6. حدّث `docs/progress.md`: ما بُني، ما تبقى، أي قرار معماري اتُخذ ولماذا

**لا تبنِ أكثر من وحدة في جلسة واحدة.**

---

# قواعد التصميم لهذا المستودع (إلزامية)

هذا المشروع يتبع نظام تصميم **Organic** بالحرف. مرجع الشاشات هو نموذج HTML في
`design_handoff_verification_platform/prototype/منصة التحقق.dc.html`، ومرجع القيم هو
`apps/console/src/styles/organic.css` (منسوخ من `design-system/styles.css`). اقرأ
`design_handoff_verification_platform/README.md` قبل تنفيذ أي شاشة.

## ممنوع

- كتابة أي لون (hex/rgb/hsl) أو مقاس مسافة أو نصف قطر أو ظل بقيمة مباشرة. كل قيمة من `var(--*)`.
- إدخال Tailwind بقيم عشوائية (`bg-[#...]`, `p-[13px]`) أو أي مكتبة مكوّنات جاهزة (MUI، Chakra، shadcn بثيمها الافتراضي) دون إعادة ربطها بالتوكنات.
- استخدام خطوط Inter أو Roboto أو Arial أو أي خط غير المعتمد.
- الاعتماد على حالات المتصفح الافتراضية (حلقة تركيز زرقاء، hover بلا تعريف).
- **تمييز لوحة المنصة عن بوابة المشترك باللون وحده.** كانت اللوحة داكنة لهذا الغرض، وأُلغي الوضع الداكن في ADR-159: ثيمٌ ثانٍ يُصان لكل مكوّن يُضاف، وسطحٌ يُقرأ كمنتج آخر لا كالوجه الثاني لهذا المنتج، وقراءةٌ أطول لمن يعيش في هذه الشاشة. التمييز الآن شريط «لوحة المنصة» أعلى كل شاشة لا يذهب بالتمرير، لأن كلمةً تُقرأ أبقى من لونٍ يُعتاد في اليوم الثاني.
- ذكر مزوّد البيانات أو إضافة أي حقل لمفاتيح API خاصة بمزوّد المشترك.

## مطلوب

- الخطوط (ADR-125): `"IBM Plex Sans Arabic"` وحده للمنصة كلها، كما تفعل المنصات الحكومية: العناوين وأسماء البطاقات 700، والأزرار 600، والنص والجداول 400/500/600. لا يُحمَّل خط آخر.
- الأنصاف: الأزرار والحقول والوسوم `999px`، البطاقات `26–28px`، إطار الشاشة `32px`.
- الألوان (ADR-122، بطابع المنصات الحكومية السعودية): الأكسنت `--color-accent` هو الأخضر الوطني لكل إجراء وتنقل ورابط وللمُتحقق منه، والذهبي `--color-gold-*` بجانبه للسلسلة الثانية والزخرفة، والرمادي البارد للنص والحدود، والكهرماني `--color-amber-*` لما يستحق النظر، والأحمر `--color-red-*` لما فشل. أدوار Organic تُوجَّه إلى هذه السلالم في `product.css` ولا تُكتب القيم في الشاشات. النص بحجم الفقرة بلون الأكسنت يستخدم `--color-accent-700`. **في البوابة البطاقات والحقول بيضاء على أرضية `--color-ground` بلمسة من الأخضر (ADR-121)**، والرمادي الفاتح `--color-inset` لما يحتاج ظلاً داخل البطاقة، والحواف `--color-hairline` مع `--shadow-sm`. واللوحة على الأرضية نفسها بدرجة أفتح، ويميّزها شريط «لوحة المنصة» لا لونها (ADR-159).
- الظلال من `--shadow-sm/md/lg` فقط.
- الأيقونات: Lucide بـ `stroke-width: 2.75`، 14–17px.
- الاتجاه: `dir="rtl"`، رؤوس الجداول محاذاة يميناً، الآيبان `direction: ltr`.
- كل عنصر تفاعلي: hover بتعبئة من سلّم الأكسنت + `:active` بدرجة أعمق + `:focus-visible { outline: 2px solid var(--color-accent); outline-offset: 2px }`.
- الانتقالات 120–180ms على الألوان فقط. **الاستثناءان:** مؤشرات التحميل (ADR-120): شريط التقدم أعلى النافذة، ومؤشر البيانات في الإطار، والضوء المار على أشكال التحميل. وخريطة التقاطعات في ملف العميل بطلب المالك (ADR-123). بقيم من توكنات `organic.css`، وتتوقف كلها مع `prefers-reduced-motion`.
- **التنقل عبر الموجّه (ADR-120):** رابط الشاشة `Link` أو `ButtonLink` أو `TagLink` لا `<a href>`، ورابط الملف `ButtonLink download`. وكل شاشة تتحقق من الجلسة بنفسها (`query()` في البوابة و`operatorOrSignIn()` في اللوحة)، لأن الـlayout لا يُعاد تنفيذه عند الانتقال.
- ابنِ طبقة مكوّنات أساسية أولاً (`Button`, `Tag`, `Input`, `Segmented`, `Card`, `Table`, `Dialog`) مطابقة لفئات النظام، ثم استخدمها في كل الشاشات: لا أزرار أو وسوم مخصّصة داخل الشاشات.
- **shadcn/ui هي مكتبة المكوّنات المعتمدة (ADR-119)**، على Base UI وTailwind v4 وبدعم RTL. مكوّناتها الأولية في `src/components/shadcn` وتصل إليها الشاشات عبر طبقة المكوّنات في `src/components/ui` لا مباشرة. متغيرات ثيمها كلها مربوطة بتوكنات Organic في `src/styles/tailwind.css`، وأصناف Tailwind لا تُكتب إلا بأسماء هذا الثيم، ولا reset من Tailwind (preflight) لأنه يغيّر ما بُني. أي مكوّن يُضاف بالـCLI يُراجَع ليطابق هذه القواعد قبل الالتزام.
- **أي تغيير في الواجهة لا يُفترض أن يحرّك التصميم** يُثبت بلقطات قبل وبعد: `pnpm design:all <dir>` ثم `pnpm design:compare <before> <after>`.
- **كل شاشة تعمل على الجوال والتابلت (ADR-126):** تحت 1024px يصير الشريط الجانبي درجاً، ولا تمرير أفقي للصفحة، والجدول الأعرض من بطاقته يُمرَّر داخلها. `pnpm design:responsive` يمر على كل الشاشات قبل إعلان أي شاشة مكتملة.
- كل شاشة تحتاج حالات: تحميل، فراغ، خطأ.
- النصوص العربية في النموذج هي النص المعتمد: لا تُعِد صياغتها.

## قبل إعلان أي شاشة مكتملة

نفّذ «قائمة الفحص النهائية» في نهاية `design_handoff_verification_platform/README.md`، وافتح النموذج بجانب التطبيق وقارن بصرياً.
