# PLAN.md: تنفيذ حزمة التصميم على المنصة القائمة

المرحلة 0 من `design_handoff_verification_platform/AUTORUN.md`. لم تُكتب أي شاشة بعد. هذا الملف يشرح كيف تُنفَّذ المراحل 1 إلى 8 داخل هذا المستودع كما هو، وما يحتاج موافقتك قبل البدء.

---

## 1. ما نُفّذ في التهيئة

| الخطوة في AUTORUN | ما حدث فعلاً |
| --- | --- |
| نسخ `CLAUDE.md` إلى الجذر | الجذر فيه `CLAUDE.md` أصلاً (القواعد الاثنتا عشرة والمعمارية وحراسها)، فأُلحقت قواعد التصميم في آخره كما تنص `START-HERE.md` («إن كان لديك CLAUDE.md أصلاً، ألحق محتوى الملف المرفق في نهايته بدل استبداله»). قسم «الواجهة» القديم أُعيدت كتابته: حُذفت منه الألوان الكحلية والتركوازية وخط IBM Plex Mono وقاعدة «لا ظلال، حدود صلبة» لأنها تناقض التصميم، وبقيت قواعد السلوك التي لا تناقضه. |
| نسخ `styles.css` إلى `src/styles/organic.css` | نُسخ حرفياً إلى `apps/console/src/styles/organic.css`. الواجهة في هذا المستودع تطبيق داخل `apps/console`، فـ`src/` هنا هو `src/` الخاص به. |
| نسخ `verify/*.mjs` | نُسخا حرفياً إلى `verify/` في الجذر. `verify/output/` مضاف إلى `.gitignore`. |
| سكربتات `package.json` | `pnpm design:check` يشغّل `check-design.mjs` من داخل `apps/console` ليكون `src/` هو مجلد الواجهة. `pnpm design:shots` يشغّل `shoot.mjs`. |
| الخطوط في `<head>` | رابط Google Fonts نفسه الموجود في الحزمة (Baloo Bhaijaan 2 بوزني 500 و700، وIBM Plex Sans Arabic من 400 إلى 700) في `apps/console/app/layout.tsx`. رابط IBM Plex Mono باقٍ مؤقتاً حتى تُستبدل الشاشات القديمة في المرحلة 2. |

**خط الأساس قبل أي تنفيذ:** تشغيل الفاحص على الواجهة الحالية كلها يعطي 34 مخالفة، كلها في `app/tokens.css` (27 لوناً مباشراً، 3 أنصاف أقطار، 4 خطوط غير معتمدة)، و23 ملفاً فيها عناصر تفاعلية دون أي تعريف لـ`:focus-visible`. الاختبارات والأسلوب وفحص الأنواع خضراء.

---

## 2. نقطة البداية: المنصة ليست فارغة

الحزمة كُتبت على افتراض أنه «لا بيئة بعد»، ولذلك تطلب طبقة `src/mocks`. الواقع هنا مختلف: الخلفية مبنية ومختبرة (79 وحدة، 775 اختباراً على بوستجرس حقيقي)، وفيها فعلاً ما ترسمه الشاشات:

- ملف العميل بأقسامه، وزر تحقق لكل قسم، ومؤشرات KYB وKYC، والتقاطعات داخل محفظة المشترك نفسه.
- طلب التحقق بصناديق الاختيار والتسعير التقديري، وتنفيذ كل منتج في معاملته.
- التصنيف: شركة، مؤسسة، عامل حر.
- لوحة إدارة منفصلة بدخول مستقل، فيها المشتركون وتواريخ انتهائهم وأرصدتهم، والباقات والأسعار، والحوالات، والربط التقني مع مصدر البيانات.
- عزل المشتركين بـ`tenant_id` وRLS على كل جدول.

لذلك الخطة: **الشكل من الحزمة، والبيانات من الخلفية الحقيقية.** لا طبقة mock. البيانات الظاهرة في الصور تُصنع بمجموعة بيانات تجريبية تمر بالمسار الحقيقي نفسه وتُوسم بأنها تجريبية.

---

## 3. تكييف العقد مع هذا المستودع (موافَق عليه)

| في AUTORUN | في هذا المستودع | السبب |
| --- | --- | --- |
| `src/components/ui` | `apps/console/src/components/ui` | مكان الواجهة. |
| `src/styles/organic.css` | `apps/console/src/styles/organic.css` | تم. |
| نقل الشاشات تحت `src/` | `app/` و`components/` و`lib/` تنتقل إلى `apps/console/src/` في المرحلة 2 (Next.js يدعم مجلد `src/`) | ليغطي `check-design.mjs` كل شاشة. بدون النقل يفحص ملفاً واحداً فينجح زوراً. |
| `src/mocks` | لا يُنشأ. بدلاً منه سكربت بيانات تجريبية يعيد إنتاج عملاء الصور عبر المسار الحقيقي | الـmock يتجاوز RLS والفوترة والـIdempotency، ويتباعد عن الحقيقة مع أول تعديل. |
| `src/api/` | لا يُنشأ. الشاشات تقرأ عبر دوال `@nx-verify/core` من خلال `src/lib/context.ts` | هذا الباب موجود، وهو الذي يضبط نطاق المشترك في كل استعلام. |
| `PROGRESS.md` | ملف جديد في الجذر لمراحل التصميم، و`docs/progress.md` يبقى سجل الوحدات ويشير إليه | الملفان يُقرآن في كل جلسة، ولكل منهما غرض. |

---

## 4. التنقل (المرحلة 2)

### بورتال المشترك: الرئيسية وأربعة أقسام

| القسم | المسار | ما يسكن داخله | يأتي من الموجود حالياً |
| --- | --- | --- | --- |
| اللوحة الرئيسية | `/dashboard` (و`/` يحوّل إليه) | الرصيد، الاشتراك، آخر العمليات، الاستهلاك، بدء تحقق سريع | `dashboard` |
| العملاء | `/customers` · `/customers/[id]` · `/customers/relations` · `/customers/alerts` | القائمة والبحث، ملف العميل بأقسامه الخمسة، التقاطعات والعلاقات، التنبيهات المفتوحة | `customers` + `monitoring` + `verifications/reviews` |
| التحقق | `/verifications/new` · `/verifications` · `/verifications/failed` · `/verifications/onboarding` | طلب تحقق جديد، كلي أو منتج واحد، سجل العمليات ونتائجها، العمليات المتعثرة وإعادتها، روابط التأهيل | `customers/new` + `verifications` + `onboarding` |
| الاشتراك والرصيد | `/billing` · `/billing/invoices` · `/billing/prices` | الباقة وتاريخ الانتهاء، الرصيد واستهلاكه، الفواتير والإيصالات، أسعار المنتجات | `billing` + `billing/statement` |
| الإعدادات | `/settings/users` · `/settings/requirements` · `/settings/notifications` · `/settings/developers` | المستخدمون والصلاحيات، الحقول المطلوبة لكل كيان، التنبيهات والإشعارات، مفاتيح الربط والـWebhooks | `settings` + `developers` + `rules` + `portfolios` |

بطاقة الرصيد أسفل الشريط الجانبي في كل صفحات البورتال. كل مسار قديم يحوّل إلى مكانه الجديد.

### لوحة الإدارة: ستة أقسام بالوضع الداكن

| القسم | المسار | المحتوى |
| --- | --- | --- |
| نظرة عامة | `/operator` | من `platformOverview` الحالية، بتخطيط مشتق من شاشة 06 |
| المشتركون | `/operator/subscribers` · `/operator/subscribers/[id]` | شاشة 06، وصفحة إدارة المشترك: الباقة، الرصيد، الحوالات، السعر الخاص، الإيقاف |
| الأسعار والمنتجات | `/operator/pricing` | شاشة 05 الجزء العلوي: سعر كل منتج، حزم الرصيد، الاشتراكات، الأسعار الخاصة |
| إعدادات التحقق | `/operator/verification` · `/operator/verification/integration` | شاشة 05 الجزء السفلي، وتبويب «الربط التقني» الذي طلبته سابقاً: مفاتيح البيئتين، الـwebhook، اختبار الاتصال، الصحة |
| التقارير | `/operator/reports` | الإيراد والتكلفة والهامش والاستهلاك، من العدادات المجمّعة فقط (القاعدة 2) |
| الصلاحيات والتدقيق | `/operator/access` | حسابات فريق الإدارة وأدوارهم، وسجل كل تغيير |

الفصل: مسارات `/operator` بدخولها المستقل كما هي الآن، مع متغير اختياري `NX_OPERATOR_HOST` لتقديم اللوحة على نطاق خاص (مثل `admin.`) إن أردت ذلك في النشر.

---

## 5. طبقة المكوّنات (المرحلة 1)

في `apps/console/src/components/ui/`، كل مكوّن غلاف رقيق فوق فئات `organic.css` بلا قيمة مباشرة واحدة:

| المكوّن | الفئات | ملاحظات |
| --- | --- | --- |
| `Button` | `.btn` + `primary / secondary / ghost / icon / block` | يقبل أيقونة Lucide، وحالة `pending` لأزرار النماذج |
| `Tag` | `.tag` + `accent / accent-2 / neutral / outline` | الحالات الدلالية (مُتحقق، قيد المعالجة، تعارض، منتهي، غير مطبّق) خريطة واحدة لا تتكرر |
| `Input` · `Field` | `.input` · `.field` | `dir="ltr"` تلقائي لحقول الأرقام والآيبان |
| `Radio` · `Segmented` | `.radio` · `.seg` | على عناصر أصلية، تعمل بلا سكربت |
| `Card` | `.card` | نصف القطر `var(--radius-lg)` (28px) كما في المواصفات |
| `Table` | `.table` | رؤوس الأعمدة يميناً |
| `Dialog` | `.dialog-backdrop` + `.dialog` | عنصر `<dialog>` أصلي |
| `Icon` | Lucide | `stroke-width` ثابت 2.75، مقاس 14 إلى 17px |

ثم مكوّنات مركّبة منها فقط، تُبنى مع أول شاشة تحتاجها: `StatCard`، `ProgressBar`، `CompletenessRing`، `SectionCard`، `ProductRow`، `Timeline`، `RelationGraph`، `Skeleton`، `EmptyState`، `ErrorState`.

اعتماد جديد واحد للواجهة: `lucide-react`. واعتماد تطوير واحد: `playwright` لسكربت التصوير. لا مكتبة مكوّنات.

---

## 6. الشاشات وشكل البيانات

### المرحلة 3: شاشة 03 ملف العميل

- **موجود:** `getCustomerFile` يعيد الأقسام وحالاتها، المدراء، الشركاء، الحسابات، المؤشرات، التقاطعات، وسجل كل حقل.
- **يُضاف (مشتقات تُحسب ولا تُخزَّن):**
  - ترقيم الأقسام وعددها حسب نوع الكيان: شركة 5، مؤسسة 4 (المدراء اختياري)، عامل حر 4 (شهادة العمل الحر بدل عقد التأسيس والمدراء).
  - اكتمال الملف من الأقسام المطلوبة لنوع الكيان في إعدادات التحقق.
  - حالة KYB بكلمة واحدة (مستوفى، ناقص، قيد الإكمال)، وعدّاد KYC «2 من 3».
  - درجة مخاطر من 0 إلى 100 بأسباب موزونة (+22 تعارض الاسم البنكي، +14 عنوان مشترك، +10 قسم غير مكتمل)، بدل المستويات الحالية.
  - نسبة مطابقة الاسم البنكي مقابل «حد تطابق الاسم المقبول».
  - عقد وحواف مخطط العلاقات من التقاطعات الحالية.
- **تصدير الملف:** مستند الأدلة الموجود في `packages/core/src/evidence` بقالب التصميم.

### المرحلة 4: شاشة 02 طلب تحقق جديد

- **موجود:** قائمة المنتجات حسب نوع الكيان، التسعير التقديري، `runChecks`.
- **يُضاف:**
  - البحث عن الكيان داخل محفظة المشترك («تم العثور على… سيُربط التحقق بملفها الحالي») بالهاش لا بالنص.
  - وسم حالة لكل منتج على هذا الكيان: مُتحقق سابقاً مع التاريخ، لم يُتحقق، قيد المعالجة، غير مطبّق، اسم غير مطابق.
  - **تنفيذ غير متزامن:** جدول `verification_jobs` بـRLS، ينفّذه العامل بـ`FOR UPDATE SKIP LOCKED`، وتستطلعه الشاشة. كل صف يتحول من «قيد المعالجة» إلى نتيجته حين يكتمل، بأي ترتيب، والقسم يُعبّأ فوراً. إعادة المحاولة التلقائية بحسب «عدد المحاولات عند الفشل» ولا تُحتسب.
  - «حفظ كمسودة»: طلب بحالة `DRAFT`، معرّفاته مشفّرة ومهشّرة (القاعدة 4).
  - إخفاء الأسعار عن مستخدمي المشترك: إعداد على مستوى المشترك.
  - حالة الرصيد الصفري: «تحقق من الكل» معطّل مع دعوة لشراء رصيد.

### المرحلة 5: شاشة 01 اللوحة الرئيسية

- **موجود:** `countRuns`، `listRecentRuns`، `buildMonthlyReport`، وبداية دالة `homeOverview` (غير مُلتزمة بعد، كُتبت قبل وصول الحزمة).
- **يُضاف:** الترحيب حسب الوقت باسم المشترك، آخر تحديث للبيانات، عملاء متحققون مع زيادة الشهر، ملفات ناقصة، تنبيهات تعارض، بطاقة الباقة، شريط «ابدأ تحققاً جديداً»، استهلاك كل منتج هذا الشهر، ملاحظة الأداء (متوسط زمن الاستجابة، ونسبة الاكتمال من المحاولة الأولى)، وتصدير التقرير.

### المرحلة 6: شاشة 04 قائمة العملاء

- **موجود:** `listCustomers` و`countCustomers` مع البحث برقم السجل عبر الهاش.
- **يُضاف:** عدّادات الشركات والمؤسسات منفصلة، عمود الاكتمال، وسم KYB، نص المخاطر، آخر تحقق، فلتر «تنبيهات مفتوحة»، والبحث بالاسم أو الآيبان.
- الصفحات المشتقة من نفس النمط في هذه المرحلة: سجل العمليات، العمليات المتعثرة، التنبيهات المفتوحة.

### المرحلة 7: شاشة 05 الأسعار وإعدادات التحقق (داكنة)

- **موجود:** `price_book` بصفوف عامة وصفوف خاصة لكل مشترك، `cost_book`، `margin_counters`، `packages`، وإتاحة المنتج وحالته.
- **يُضاف:**
  - تعديل السعر من الجدول: صف جديد بتاريخ سريان، لا تعديل للقديم. الحارس 10 يرفض أي سعر تحت التكلفة، وتحذير تحت هامش 30%.
  - حزم الرصيد مسبقة الدفع: جدول `credit_bundles` ومنح بصلاحية 12 شهراً.
  - الاشتراكات: رسم شهري وعدد عمليات وسعر تجاوز.
  - الأسعار الخاصة: قيم متجاوِزة لكل منتج أو خصم نسبي على الكل.
  - إعدادات التحقق: جدول منصة واحد (المحاولات 2، الصلاحية 90 يوماً، حد التطابق 85%، تنبيه انتهاء السجل 30 يوماً)، وجدول الأقسام المطلوبة لكل نوع كيان (إلزامي أو اختياري). كلها مسجّلة في سجل التدقيق.
  - «آخر تعديل بواسطة…» و«سجل التغييرات» يحتاجان حسابات مسمّاة لفريق الإدارة (القرار 5).
- الصفحات المشتقة: تبويب الربط التقني، وإعدادات البورتال.

### المرحلة 8: شاشة 06 المشتركون والأرصدة (داكنة)

- **موجود:** `listSubscriberSummaries` و`platformOverview` و`getSubscriberDetail`.
- **يُضاف:** إيراد الشهر ومقارنته بالشهر السابق، رصيد غير مستهلك عبر المشتركين، بطاقة «تحتاج إجراء»، عمود السعر الخاص، حالة «موقوف»، التصدير، ونموذج «مشترك جديد» بدل سكربت التهيئة.
- الصفحات المشتقة: نظرة عامة، التقارير، الصلاحيات والتدقيق.

كل جدول جديد: ترحيل up وdown، RLS إن حمل `tenant_id`، واختبارات على بوستجرس حقيقي.

---

## 7. القرارات (وافق عليها المالك في 2026-09-14)

1. **تكييف المسارات** في القسم 3: الواجهة تحت `apps/console/src/`، وبيانات تجريبية حقيقية بدل `src/mocks`، ولا `src/api`.
2. **الشرطة الطويلة (em dash).** نصوص النموذج فيها 38 منها، و`CLAUDE.md` يمنعها في أي نص، وفحص `pnpm style` يرفضها. أقترح إبقاء القاعدة واستبدال الشرطة وحدها بـ«·» أو «،» أو «:» دون تغيير أي كلمة من النص المعتمد.
3. **وحدة الرصيد.** التصميم يعدّ الرصيد بالعمليات («1,840 عملية تحقق») ويعرض السعر بالريال. أقترح نموذجاً من جزأين يوافق ما هو مبني: عمليات من الباقة أو الحزمة تُستهلك أولاً، ثم محفظة بالريال للتجاوز بسعر كل منتج. البديل: رصيد بالريال فقط، وهذا يغيّر نصوص التصميم.
4. **الأسعار والفشل.**
   - أرقام الصور أمثلة لا تغطي التكلفة الحقيقية: السجل التجاري في الصورة 3.00 ريال، وتكلفة النداء علينا 10 ريالات. ستبقى الأسعار الحقيقية، والحارس 10 يمنع البيع تحت التكلفة.
   - «العمليات الفاشلة لا تُحسب» مطبّق أصلاً على الأعطال التقنية.
   - يبقى سؤال: هل «لا يوجد سجل بهذا الرقم» عملية ناجحة تُحسب أم فاشلة؟ أقترح اعتبارها ناجحة لأن الجهة أجابت ونحن ندفع تكلفتها.
5. **حسابات مسمّاة لفريق الإدارة** بأدوار (مالك، تسعير، دعم، قراءة فقط) بدل رمز الدخول المشترك الحالي. يبقى الرمز لإنشاء أول مالك فقط. هذا ما تحتاجه عبارة «آخر تعديل بواسطة» وقسم «الصلاحيات والتدقيق».
6. **التنفيذ غير المتزامن** بطابور وظائف في قاعدة البيانات والعامل الموجود، كما في المرحلة 4.
7. **توكنات إضافية في `organic.css`.** المواصفات تعطي مقاسات الإطار وألوان الوضع الداكن بقيم مباشرة (`#25221d`، `#2b2823`، `244px`…)، والعقد يمنعها خارج ملف التوكنات. أقترح إلحاقها كتلة موسومة في آخر `organic.css` دون تعديل أي توكن أصلي.
8. **العنوان الوطني للعامل الحر.** منتجات مصدر البيانات الحالية تتحقق من عنوان المنشأة برقمها الموحد فقط، ولا منتج لعنوان فرد. أقترح أن يظهر القسم عند العامل الحر بحالة «غير متاح لهذا النوع» حتى يتوفر منتج.
9. **اسم المنصة.** يبقى **NX Trust**، ويُكتب في مكان واحد حتى يتغير دون المساس بالشاشات.

---

## 8. التحقق بعد كل مرحلة من 2 إلى 8

1. `pnpm design:check` حتى صفر مخالفات.
2. `pnpm design:shots` بعرض 1440، مع تعديل `ROUTES` في `shoot.mjs` إلى المسارات أعلاه كما يطلب تعليقه، وتمرير جلسة المشترك وجلسة الإدارة ككوكيز. ثم مقارنة كل صورة بنظيرتها في `screens/`، وتدوين كل فرق وإصلاحه في `PROGRESS.md`.
3. «قائمة الفحص النهائية» في آخر `README.md`، بندًا بندًا بنعم أو لا. البند الذي لا يخص المرحلة يُكتب «لا ينطبق» مع السبب. لا تُعلن مرحلة وفيها «لا».
4. بوابات المستودع نفسها: `pnpm test` بحراسه العشرة، `pnpm lint`، `pnpm style`، `pnpm typecheck`.
5. التزام واحد لكل مرحلة بعد نجاح كل ما سبق.

**المرحلة 2 تحديداً** تنقل الواجهة تحت `src/`، فيصبح الفاحص يرى `tokens.css` القديم. لذلك تستبدله بطبقة انتقالية: أسماء المتغيرات القديمة تشير إلى توكنات Organic. تبقى الشاشات القديمة تعمل بالألوان الجديدة إلى أن يأتي دور كل منها، ويخرج الفاحص بصفر من أول مرحلة.
