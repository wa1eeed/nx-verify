import js from '@eslint/js';
import tseslint from 'typescript-eslint';

export default tseslint.config(
  {
    ignores: [
      '**/node_modules/**',
      '**/dist/**',
      '**/.next/**',
      '**/.next-check/**',
      '**/coverage/**',
      '**/.turbo/**',
      // Generated by Next on every build, and not ours to edit.
      '**/next-env.d.ts',
      // The design handoff is a vendored reference, kept exactly as it was delivered.
      'design_handoff_verification_platform/**',
    ],
  },
  js.configs.recommended,
  ...tseslint.configs.strict,
  {
    languageOptions: {
      parserOptions: {
        ecmaVersion: 2022,
        sourceType: 'module',
      },
    },
    rules: {
      // Rule 4 and rule 10: identifiers and credentials must never reach a log sink.
      'no-console': ['error', { allow: ['error'] }],
      '@typescript-eslint/no-explicit-any': 'error',
      '@typescript-eslint/consistent-type-imports': 'error',
    },
  },
  {
    // Migration runner and operational scripts report progress on stdout by design.
    files: [
      'packages/db/scripts/**/*.ts',
      'scripts/**/*.ts',
      'test/global-setup.ts',
      'apps/api/src/server.ts',
      'apps/mcp/src/server.ts',
      'apps/worker/src/main.ts',
    ],
    rules: {
      'no-console': 'off',
    },
  },
  {
    // The design checks from the handoff: plain Node scripts that print a report.
    files: ['verify/**/*.mjs'],
    languageOptions: {
      globals: { process: 'readonly', console: 'readonly' },
    },
    rules: {
      'no-console': 'off',
    },
  },
);
