# PROGRESS.md: مراحل حزمة التصميم

يُحدَّث بعد كل مرحلة، كما يطلب `design_handoff_verification_platform/AUTORUN.md`. لكل مرحلة: ما بُني، والفحوص بأرقامها، والفروق مع المرجع وكيف أُصلحت، و«قائمة الفحص النهائية» بنعم أو لا. الخطة في `PLAN.md`، والقرار المعماري في ADR-113.

---

## المرحلة 0 · التهيئة والخطة · ✅ 2026-09-14

- قواعد التصميم أُلحقت بآخر `CLAUDE.md`، وقسم الواجهة القديم بقي منه ما لا يناقضها.
- `apps/console/src/styles/organic.css` هو `design-system/styles.css` كما سُلّم.
- `verify/check-design.mjs` و`verify/shoot.mjs` كما سُلّما. `pnpm design:check` يشغّل الأول من داخل `apps/console`، و`pnpm design:shots` الثاني.
- خطوط الحزمة في `<head>`.
- `PLAN.md` كُتب، ووافق المالك على قراراته التسعة، والاسم يبقى NX Trust.

**قبل التهيئة:** فحص التنسيق في CI كان أحمر على 143 ملفاً. نُسّقت في التزام مستقل، والمجموعة كاملة (775 اختباراً) مرت بعده.

---

## المرحلة 1 · طبقة المكوّنات · ✅ 2026-09-14

### ما بُني

| الملف | المكوّن | الفئات |
| --- | --- | --- |
| `button.tsx` | `Button` · `ButtonLink` · `IconButton` | `.btn` + `primary / secondary / ghost / icon / block` |
| `submit-button.tsx` | `SubmitButton` | زر نموذج إجراء الخادم، يُعطَّل ويُوسم مشغولاً أثناء التنفيذ |
| `tag.tsx` | `Tag` · `StateTag` · `STATE_TONES` | `.tag` + `accent / accent-2 / neutral / outline / critical` |
| `input.tsx` · `field.tsx` | `Input` · `Field` | `.input` · `.field`، والتسمية والتلميح والخطأ مربوطة بالحقل |
| `checkbox.tsx` · `radio.tsx` | `Checkbox` · `Radio` | `.check` (جديد) · `.radio` |
| `segmented.tsx` | `Segmented` | `.seg` + `.seg-opt` |
| `card.tsx` | `Card` · `CardTitle` · `CardKicker` · `CardBody` · `CardMeta` | `.card` + `panel / stat` وأدوار التلوين |
| `table.tsx` | `Table` · `Th` | `.table` داخل حاوية تمرير |
| `dialog.tsx` | `Dialog` | `.dialog` فوق `.dialog-backdrop` على عنصر dialog الأصلي |
| `icon.tsx` | `Icon` | Lucide بسماكة 2.75، والباب الوحيد إليها |
| `ltr.tsx` | `Ltr` | رقم أو معرّف أو آيبان معزول باتجاه LTR |

ومعها:

- **كتلة توكنات ملحقة** في آخر `organic.css`: أنصاف 6 و14 و18 و20 و26 و32، ومقاسات الإطار والشريط الجانبي، والحشوات، ومقاسات الخط، والخطان، ومدة الانتقال، وألوان الوضع الداكن.
- **`src/styles/product.css`** بتوكنات فقط: تجاوزات النموذج نفسه (الخطوط، رؤوس الجداول، الروابط)، وما لا تحمله الورقة من حالات، والوضع الداكن تحت `[data-theme='dark']`.
- **`lucide-react`** اعتماداً للواجهة.

### الفحوص

| الفحص | النتيجة |
| --- | --- |
| `pnpm design:check` | 17 ملفاً، **صفر مخالفات**، والملفات التفاعلية الستة كلها تعرّف `:focus-visible` |
| `pnpm design:shots` | لا ينطبق بعد: لا مسارات حتى المرحلة 2. بدلاً منه صُيِّرت المكوّنات كلها في صفحة واحدة بالوضعين الفاتح والداكن، وصُوّرت بعرض 1440، وقورنت بالشاشات 01 و02 و03 و05 و06 |
| اختبارات المكوّنات | 27 تمر، ومنها فحص التصميم نفسه، وأن الورقة تبدأ بالملف المسلَّم بايتاً ببايت، وأن لكل سطح ملوّن تعبئة داكنة |
| المجموعة كاملة | 802 اختبار في 75 ملفاً، والحراس العشرة خضر |
| lint · typecheck · style · format | خضراء |

### الفروق مع المرجع وكيف أُصلحت

1. **الوضع الداكن:** بطاقتا «باقة النمو» و«درجة المخاطر» احتفظتا بتعبئتهما الفاتحة تحت نص فاتح، فاختفت كلماتهما في اللقطة. أُعطيت كل تعبئة ملوّنة نظيرتها الداكنة من السلّم نفسه (`accent-900` و`accent-2-900`)، واختبار يرفض سطحاً ملوّناً بلا تعبئته الداكنة.
2. **فاصل الشرائح:** الورقة ترسمه على اليسار، وفي سطر عربي يقع على الحافة الخارجية لا بين الخيارات. صار على الجانب المنطقي `border-inline-start`.
3. **نصف قطر البطاقة:** الورقة تجعله 1.15 من الكبير (32px تقريباً)، والمواصفات تطلب 26 إلى 28. البطاقة 28، والبطاقة الإحصائية 26.
4. **تباعد حروف العناوين:** الورقة تضيّقه (`-0.015em`)، وهذا يفصل الحروف العربية المتصلة. صار صفراً كما في كتلة الأنماط داخل النموذج.
5. **مربع الاختيار:** في النموذج رسمة ثابتة لا عنصر إدخال. صار عنصر إدخال أصلياً مخفياً داخل تسميته، بالرسم نفسه: 18px ونصف قطر 6 وتعبئة الأكسنت وعلامة 13px.
6. **الحوار:** الورقة تبنيه من عنصرين عاديين. صار عنصر dialog الأصلي هو الخلفية، فيأتي حبس التركيز وEscape وتعطيل الصفحة خلفه من المتصفح. اللقطة أكدت أن التركيز يدخل ويُرسم بحلقة الأكسنت.
7. **الخطوط:** Caprasimo وFigtree في الورقة لا يحملان العربية. العناوين والأزرار بـBaloo Bhaijaan 2 والنص بـIBM Plex Sans Arabic كما تنص المواصفات، بتوكنات ملحقة لا بتعديل توكنات الورقة.

### قائمة الفحص النهائية

| البند | الجواب |
| --- | --- |
| لا لون ولا مقاس بقيمة مباشرة خارج ملف التوكنات | **نعم** في كل ما تحت `src/`. الشاشات القديمة خارج `src/` حتى تنتقل في المرحلة 2 |
| الأزرار والحقول والوسوم 999px، والبطاقات 26 إلى 28، والإطار 32 | **نعم** للمكوّنات. توكن الإطار موجود، والإطار نفسه في المرحلة 2 |
| العناوين Baloo Bhaijaan 2 والنص IBM Plex Sans Arabic، ولا Inter ولا Roboto ولا Arial | **نعم** |
| لكل عنصر تفاعلي hover و`:active` و`:focus-visible` | **نعم**. أُضيفت حالات الضغط التي تنقص الورقة للراديو والشرائح والروابط، وحالات مربع الاختيار كلها |
| `dir="rtl"`، ورؤوس الجداول يميناً، والآيبان LTR | **نعم** |
| أيقونات Lucide بسماكة 2.75 فقط | **نعم**، واختبار يمنع أي ملف غير `icon.tsx` من استيراد المكتبة |
| زر تحقق لكل قسم، وزر لكل منتج مع «تحقق من الكل» | **لا ينطبق** في هذه المرحلة: المرحلتان 3 و4 |
| لا ذكر لمزوّد البيانات ولا حقل لمفاتيح مزوّد خاص بالمشترك | **نعم** |
| لوحة الإدارة بالوضع الداكن المحدّد | **نعم** للمكوّنات، ومصوّرة. تطبيقه على اللوحة في المرحلة 2 |
| حالات التحميل والفراغ والخطأ في كل شاشة | **لا ينطبق**: لا شاشات في هذه المرحلة. المكوّنات تحمل حالة الانشغال والخطأ |

---

## المرحلة 2 · القشرة والتنقل · ✅ 2026-09-14

### ما بُني

- **الواجهة تحت `apps/console/src/`** (`app` و`components` و`lib`)، فصار فحص التصميم يرى 149 ملفاً لا ملفاً واحداً.
- **إطار البورتال** كما في «قالب الإطار المشترك»: الإطار المستدير على اللوحة، والشريط الجانبي بعرض 244px فيه العلامة والأقسام بأيقوناتها، وبطاقة الرصيد في أسفله (عمليات مع شريط التقدّم إن كانت هناك باقة، وريالات قبل الضريبة إن لم تكن)، ثم بيئة المساحة وزر الخروج.
- **إطار لوحة الإدارة الداكن** على الأساس نفسه تحت `data-theme="dark"`، بالعلامة «NX Trust · أدمن».
- **التنقل:**
  - البورتال: اللوحة الرئيسية، العملاء، التحقق، الاشتراك والرصيد، الإعدادات. ولكل قسم تبويباته بشكل وسوم الشاشة 04.
  - الإدارة: نظرة عامة، المشتركون، الأسعار والمنتجات، إعدادات التحقق، التقارير، الصلاحيات والتدقيق.
- **مسارات جديدة:**
  - «التقاطعات والعلاقات» `/customers/relations`.
  - «التنبيهات المفتوحة» `/customers/alerts`، ودُمج فيها مركز الإشعارات مع المراقبة.
  - «العمليات المتعثرة» `/verifications/failed`.
  - «أسعار المنتجات» `/billing/prices`، بالسعر قبل الضريبة ومعها.
  - «الصلاحيات والتدقيق» `/operator/access` من `operator_audit`.
- **كل مسار قديم يحوّل إلى مكانه**، ومنها كل مسارات الوحدة 76.
- **رأس الشاشة** بالمقاسات المعتمدة: عنوان 32px وسطر شرح بالسلّم المحايد.
- **حالات التحميل والخطأ** لكل شاشة في البورتال واللوحة، وصفحة «غير موجودة».
- **`legacy.css`:** أصناف الكونسول القديمة ما زالت تعمل، لكن كل قيمها توكنات Organic ومسمّاة على الوضعين. الأزرار القديمة صارت أزرار النظام (`btn btn-primary`) في 42 موضعاً.
- **اسم المنتج** في مكان واحد (`PRODUCT_NAME`)، والعلامة مكوّن واحد في كل الإطارات وصفحات الدخول.
- **`verify/shoot.mjs`** بمسارات هذا المستودع كما يطلب تعليقه، مع كوكي جلسة الإدارة مشتقاً كما يشتقه الكونسول، وChrome المثبّت بدل تنزيل متصفح. و`playwright` اعتماد تطوير.

### الفحوص

| الفحص | النتيجة |
| --- | --- |
| `pnpm design:check` | 149 ملفاً، **صفر مخالفات** |
| `pnpm design:shots` | ست لقطات بعرض 1440، وقورنت إطاراتها بالشاشات 01 و04 و06 |
| المجموعة كاملة | 810 اختبارات تمر، والحراس العشرة خضر |
| `pnpm build:console` | ينجح بلا تحذير |
| lint · typecheck · style · format | خضراء |

### الفروق مع المرجع وكيف أُصلحت

1. **روابط الوضع الداكن:** الأقسام غير المفتوحة والعلامة ظهرت بلون الأكسنت لا بالنص الفاتح كما في الشاشة 06، لأن قاعدة لون الروابط على الداكن أرجح من صنفها. أُعطيت نظيرتها على الداكن، وأكّدته اللقطة الثانية.
2. **رابط بشكل زر:** لون الرابط عند المرور كان سيطلي كلمات الزر الأساسي بلون تعبئته فتختفي. صار لكل رابط بشكل زر لونه في كل حالة.
3. **الزر الصغير:** حشوة `.btn` كانت تغلب `.btn-small` لأن الورقة تُحمَّل بعده. صار المحدِّد `.btn.btn-small`.
4. **الطباعة:** `border-radius: 0` في قاعدة الطباعة خالف الفحص، فحُذف.
5. **مقاسات بالبكسل** بقيت في الورقتين (مقاسات الخط الصغيرة، حشوة الوسوم، ارتفاع الحقول). صارت توكنات مسمّاة في الكتلة الملحقة، وما بقي بالبكسل حدود 1px وحلقة التركيز المحددة في المواصفات ونقاط التوقف، لأن CSS لا يقبل متغيراً فيها.
6. **فروق مقصودة ومبقاة:**
   - هامش 16px حول الإطار حتى تظهر زواياه وظله في نافذة المتصفح.
   - ملاحظة بيئة الاختبار (الحارس 09 والوحدة 41).
   - عدّاد التنبيهات على قسم العملاء.
   - سطر البيئة وزر الخروج أسفل الشريط.
   - محتوى الشاشات نفسها ما زال الكونسول السابق بألوان النظام، حتى تُبنى في المراحل 3 إلى 8.

### قائمة الفحص النهائية

| البند | الجواب |
| --- | --- |
| لا لون ولا مقاس بقيمة مباشرة خارج ملف التوكنات | **نعم**: صفر في الفحص، ولا لون ولا ظل بقيمة مباشرة في الورقتين باختبار، والمقاسات توكنات |
| الأزرار والحقول والوسوم 999px، والبطاقات 26 إلى 28، والإطار 32 | **نعم**، والإطار بنصف قطر `--radius-frame` |
| العناوين Baloo Bhaijaan 2 والنص IBM Plex Sans Arabic، ولا Inter ولا Roboto ولا Arial | **نعم**، وحُذف IBM Plex Mono من المستند، واختبار يمنع عودته |
| لكل عنصر تفاعلي hover و`:active` و`:focus-visible` | **نعم** للإطار والأقسام والتبويبات والروابط والأزرار القديمة بعد تحويلها |
| `dir="rtl"`، ورؤوس الجداول يميناً، والآيبان LTR | **نعم** |
| أيقونات Lucide بسماكة 2.75 فقط | **نعم** |
| زر تحقق لكل قسم، وزر لكل منتج مع «تحقق من الكل» | **لا ينطبق**: المرحلتان 3 و4 |
| لا ذكر لمزوّد البيانات ولا حقل لمفاتيح مزوّد خاص بالمشترك | **نعم**. نص ملاحظة بيئة الاختبار كان يقول «مزوّد وهمي» وصار «البيانات هنا تجريبية» |
| لوحة الإدارة بالوضع الداكن المحدّد | **نعم**، إطاراً وصفحة دخول |
| حالات التحميل والفراغ والخطأ في كل شاشة | **نعم** على مستوى الإطار: هيكل تحميل بأنصاف البطاقات، وخطأ بسببه وإعادة المحاولة، وحالة فراغ تقبل إجراءها |
